data:
  raw_data_path: data/raw/train.csv

split:
  method: date
  split_date: "2015-04-30"
  validation_days: 90

logging:
  level: INFO

features:
  calendar: true
  lag: true
  rolling: true
  promo: true

model:
  name: hist_gradient_boosting